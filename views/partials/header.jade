header
    nav.navbar.navbar-default
        .container
            .navbar-header
                button.navbar-toggle.collapsed(type="button", data-toggle="collapse", data-target="#bs-example-navbar-collapse-1", aria-expanded="false")
                    span.sr-only Toogle navigation
                    span.icon-bar
                    span.icon-bar
                    span.icon-bar
                a.navbar-brand(href="/")
                    img(src="/images/icon.png", alt="icon", style="max-height: 100%")

            .collapse.navbar-collapse(id="bs-example-navbar-collapse-1")
                ul.nav.navbar-nav.navbar-right
                    li
                        form.navbar-form(role="search", name="searchForm")
                            div.form-group
                                input.form-control(type="search", name="searchField")
                            button.btn.btn-default(type="submit") Поиск
                    if user
                        li: a.label.label-warning(href='/adEdit')
                        li.dropdown
                            a.dropdown-toggle(data-toggle="dropdown", role="button", aria-haspopup="true", aria-expanded="false") #{user.username}
                            ul.dropdown-menu
                                li: a(href='/myaccount') Профиль
                                li.divider(role="separator")
                                li: a.logout-btn(href="#") Выйти
                    else
                        li: a(href="/login") Войти
                        li: a(href="/register") Регистрация







